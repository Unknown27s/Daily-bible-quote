/**
 * API Configuration
 * Store your API keys and endpoints here
 */

export const API_CONFIG = {
    // General Quotes API (Quotable - no key required)
    QUOTABLE_API: 'https://api.quotable.io',

    // Bible API (bible-api.com - no key required)
    BIBLE_API_ENDPOINT: 'https://bible-api.com',
}
